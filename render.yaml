# Render blueprint: this web service + a Neon Postgres project.
# Paste the Neon connection string into DATABASE_URL after the first deploy.
services:
  - type: web
    name: llm-usage-tracker
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      # Marks this as a deployed environment: the app refuses to start on the
      # built-in dev ADMIN_TOKEN / SECRET_KEY (both generated below).
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_TOKEN
        generateValue: true
      - key: SECRET_KEY
        generateValue: true
      # Admin login front door. Required in a deployed ENVIRONMENT.
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        generateValue: true
      # Live upstream calls are enabled — but only for a virtual key whose owner
      # has attached their own provider API key on the Account page, and only up
      # to that account's monthly cap (<= $LIVE_CAP_MAX_USD). No server-side
      # provider key is set below, so nothing is billed until someone attaches one.
      - key: ENABLE_LIVE
        value: "true"
      # Don't store prompt/response bodies by default.
      - key: LOG_BODIES
        value: "false"
      - key: OPENAI_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false

# ---- all-Render fallback (Render free Postgres is deleted 30 days after creation) ----
# databases:
#   - name: llm-usage-tracker-db
#     plan: free
#     databaseName: llmtracker
#     user: llm
# Then replace the DATABASE_URL entry above with:
#       - key: DATABASE_URL
#         fromDatabase:
#           name: llm-usage-tracker-db
#           property: connectionString
